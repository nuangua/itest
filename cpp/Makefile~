VPATH=include:src:usb:ping:call:sms:ftp:udp:tcp:testcases

objects=itest.o Com.o

itest:$(objects)
	g++ -o itest $(objects)

$(filter %.o,$(objects)): %.o: %.cpp
	g++ -c $< -o $@

.PHONY:clean
clean:
	-rm -f itest *.o
